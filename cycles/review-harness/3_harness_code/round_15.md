# Круг 15 (третий прогон: код обвязки) — проход критика (sol) и исходы

Дата: 2026-09-01. Версия артефакта: после правок круга 14 (110 тестов).
Тренд: 0 находок новой территории / 2 потомка прежних правок.
Вердикт: не готова к остановке; решает оператор.

## Исходы (разработка; оба — рабочий слой; 113 тестов, pytest exit 0)

1. Маркер завершённости вне типовых и исторических инвариантов —
   **согласен**: тип completed проверяется (строковое «false» — отказ по
   контракту), а префикс истории меряется по ЗАВЕРШЁННЫМ кругам после
   фильтрации («круг 1 не завершён, круг 2 завершён» — громкое
   повреждение, а не повторный разбор старого круга).
2. Ремонт журнала принимает промежуточный ответ за финальный —
   **согласен**: совпадение ищется по точным строкам пары (вопрос И ответ
   как целые строки журнала), «да» внутри «да?» больше не считается
   записанным.

## Проход критика дословно

Полный проход круга 15 выполнен. Тесты: 110 passed за 86,07 с, код
выхода 0.

1. [ВЫСОКАЯ, подтверждено] Новый маркер завершённости не включён в типовые
и исторические инварианты метаданных: completed читается через bool(...),
тип не контролируется — строка "false" считается истинным маркером;
сценарий обхода вопроса оператора повторяется. Префикс истории проверяется
до фильтрации по маркерам, но не после: «круг 1 не завершён, круг 2
завершён» принимается и запускает повторный разбор старого круга поверх
поздней истории. Класс: блокер MVP, порча данных; потомок маркера
круга 14.

2. [СРЕДНЯЯ, подтверждено] Ремонт журнала может принять промежуточный
ответ за финальный: наличие записи определяется вхождением подстроки —
финальный маркер «Ответ …: да» находится внутри промежуточного
«Ответ …: да?», ремонт молчит, журнал навсегда без финального согласия.
Класс: история решений; потомок ремонта круга 14.

Тренд: 0 находок на новой территории / 2 потомка прежних правок.
Вердикт: не готова; финализацию решает только оператор.
