# Круг 21 (третий прогон: код обвязки) — проход критика (sol) и исходы

Дата: 2026-09-02. Версия артефакта: после правок круга 20 (123 теста).
Тренд: 0 находок новой территории / 1 потомок прежних правок.
Вердикт: не готова к остановке; решает оператор.

## Исходы (разработка; рабочий слой; 125 тестов, pytest exit 0)

1. Ответы до прохода — **согласен**, потомок моего же недочёса круга 20
   (закрыл ось «номер вне истории», объявил закрытым класс целиком —
   пропустив ось «валидный номер без прохода»). Мерило теперь проход:
   файлы ворот легальны только у кругов с записанным проходом; ответы
   без запросов (штатно остающиеся после обслуженной пачки) разбираются
   структурно на восстановлении. Тесты: фантомный ответ круга 1 в свежем
   прогоне — отказ; битые осиротевшие ответы — отказ; валидные — легальны.

## Проход критика дословно

Полный проход круга 21 выполнен. Проверка будущих и нулевых файлов
ворот работает, но чёс класса осиротевших ответов закрыт не полностью.
Тесты: 123 passed за 91,43 с, код выхода 0.

1. [ВЫСОКАЯ, подтверждено] Файл ответов текущего номера принимается
даже до появления прохода и запроса этого круга. Допустимый номер
вычисляется как следующий после завершённого префикса независимо от
наличия прохода (runcat.py); ответы без файла запросов вообще не
разбираются структурно, поскольку обход идёт только по запросам
(runcat.py). Сценарий: в новом прогоне из-за порчи появляется
round_01_gate_answers.json; восстановление успешно, критик проводит
первый проход, headless-разработка читает готовый ответ и пишет исходы,
не создавая запроса. Цикл не видит req_path, не обращается к оператору
и завершает круг с ответом, который не относится ни к одному заданному
вопросу. Класс: блокер минимально жизнеспособной версии, подмена слова
оператора при порче собственных данных; потомок правки круга 20.

Тренд: 0 находок на новой территории / 1 потомок прежних правок.
Вердикт: не готова; финализацию решает только оператор.
