# Круг 13 (третий прогон: код обвязки) — проход критика (sol) и исходы

Дата: 2026-09-01. Версия артефакта: после правок круга 12 (105 тестов).
Тренд: 2 находки новой территории / 2 потомка прежних правок.
Вердикт: не готова к остановке; решает оператор.

## Исходы (разработка; все — рабочий слой; 105 тестов, pytest exit 0)

1. Журнал нарушил первичность состояния — **согласен**: порядок
   восстановлен — сначала долговечная запись флага с дословным ответом
   (mark_entry_confirmed), затем производный журнал с фактическим текстом
   вопроса; журналирует вызывающий, ровно один раз; ask_yes_no_verbatim
   возвращает (вердикт, ответ, фактический текст) и финал не журналирует.
2. Удаления вне контракта — **согласен**: unlink запросов, временных
   журналов и partial — через контрактный помощник с причиной и действием.
3. Таймаут пробы лаунчера — **согласен**: TimeoutExpired превращается в
   отказ «лаунчер не доставлен» с действием.
4. Отказ оживления скрывает неподтверждённую смерть — **согласен**:
   результат добития учитывается, неподтверждённость называется в отказе
   с действием «добей вручную».

## Проход критика дословно

Полный проход круга 13 выполнен. Тесты: 105 passed за 80,51 с, код
выхода 0.

1. [ВЫСОКАЯ, подтверждено] Исправление журнала нарушило первичность
состояния входных ворот: финальная пара пишется в gates.md внутри gates.py,
а флаг подтверждения — только позже в cycle.py. Обрыв между ними —
повторный вопрос на уже полученное слово оператора. Класс: блокер MVP;
потомок находки круга 12.

2. [ВЫСОКАЯ, подтверждено] Контракт файловых отказов не охватывает
удаления: unlink файла запросов, временных журналов, partial. Сценарий:
ответы записаны, удаление запросов получает PermissionError — сырой
traceback, цикл заклинен. Класс: блокер MVP; потомок.

3. [СРЕДНЯЯ, подтверждено] Таймаут пробы лаунчера выходит сырым
исключением: ловится только OSError, не TimeoutExpired. Класс: MVP; новая
территория.

4. [СРЕДНЯЯ, подтверждено] Ветка отказа оживления Windows-процесса
скрывает неподтверждённое убийство: результат tree.kill() отбрасывается.
Класс: MVP, внутри модели угроз; новая территория.

Тренд: 2 находки на новой территории / 2 потомка прежних правок.
Вердикт: не готова; финализацию решает только оператор.
