# Круг 14 (третий прогон: код обвязки) — проход критика (sol) и исходы

Дата: 2026-09-01. Версия артефакта: после правок круга 13 (105 тестов).
Тренд: 1 находка новой территории / 2 потомка прежних правок.
Вердикт: не готова к остановке; решает оператор.

## Исходы (разработка; все — рабочий слой; 110 тестов, pytest exit 0)

1. Исходы разработки принимаются за завершение круга; обход ворот —
   **согласен, настоящая дыра**: введён маркер завершённости, который
   ставит САМА обвязка долговечной записью в мету круга — после
   обслуживания висящих запросов, отклонения повторной подачи и
   пере-долговечивания исходов. Восстановление считает круг завершённым
   только по маркеру; обрыв до маркера оставляет круг идущим, и перезапуск
   доводит его: живой тест воспроизводит сценарий целиком (исходы + запрос
   записаны, обвязка оборвалась — после рестарта вопрос ДОСТАВЛЕН
   оператору, и только потом круг закрыт).
2. Риск навсегда неполного журнала — **согласен**: состояние входных ворот
   хранит и дословный ответ, и фактический текст вопроса; при следующем
   входе журнал дочинивается из состояния идемпотентно.
3. Отказные ветки круга 13 без тестов — **согласен**: добавлены тесты
   ошибки удаления (контрактный отказ), таймаута пробы лаунчера
   («не доставлен»), отказа оживления с неподтверждённым добитием (оба
   факта в тексте отказа). Итог: 110 passed, pytest exit 0.

## Проход критика дословно

Полный проход круга 14 выполнен. Все четыре заявленных исправления
присутствуют. Тесты: 105 passed за 82,03 с, код выхода 0.

1. [ВЫСОКАЯ, подтверждено] Восстановление принимает неподтверждённый файл
исходов за завершённый круг и может обойти ворота оператора: restore()
считает любой непустой round_NN_outcomes.md завершением, хотя
headless-разработка создаёт его обычной записью, а обвязка обязана признать
круг только после собственной долговечной перезаписи; pending_gate_rounds
циклом не используются. Сценарий: разработка записала исходы и запрос к
оператору, обвязка оборвалась до проверки запроса — при перезапуске круг
числится завершённым, запускается следующий критик, семантический вопрос
остаётся недоставленным. Класс: блокер MVP; новая территория.

2. [СРЕДНЯЯ, подтверждено] Новый порядок входных ворот заменил повторный
вопрос риском навсегда неполного журнала: флаг пишется раньше журнала;
упавшая запись gates.md не восстанавливается — журнал навсегда без
финального ответа. Класс: дефект истории решений; потомок круга 13.

3. [СРЕДНЯЯ, подтверждено] Отказные ветки круга 13 тестами не покрыты:
нет проверок ошибки _unlink_contract, TimeoutExpired пробы лаунчера,
сочетания отказа оживления с неподтверждённым убийством. Класс: MVP и
сопровождение; потомок.

Тренд: 1 находка на новой территории / 2 потомка прежних правок.
Вердикт: не готова; финализацию решает только оператор.
