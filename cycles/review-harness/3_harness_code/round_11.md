# Круг 11 (третий прогон: код обвязки) — проход критика (sol) и исходы

Дата: 2026-09-01. Версия артефакта: после правок круга 10 (98 тестов).
Тренд: 0 находок новой территории / 5 потомков прежних правок — первый
круг без новой территории. Вердикт: не готова; решает оператор.

Параллельно кругу оператор зафиксировал три решения о глубине гарантий
(записаны блоком в описании): завершённые круги несгораемы / текущий после
аварийной перезагрузки переигрываем; история — сырьё профиля и фактологии,
не нотариат и не секрет; языки и переносимость — долг публикации задачами
графа (4b87510f, c5d146b9).

## Исходы (разработка; все — рабочий слой; 105 тестов, pytest exit 0)

1. Контракт отказов неполон в _rewrite буфера — **согласен**: перезапись
   целиком обёрнута; текст отказа честно называет допустимость повторной
   отправки пары (по решению оператора о глубине); mkdir каталога стримов
   тоже по контракту.
2. Пивот privacy довычищен не везде — **согласен**: буфер в спеке и
   докстрингах приведён к той же семантике гигиены, что каталоги.
3. Raw-свидетельство исчезает молча — **согласен**: на успешном пути
   провал записи raw — громкий отказ; на отказном пути исходный отказ
   важнее (best-effort, задокументировано).
4. Финальный ответ журналируется с неверным вопросом — **согласен**: после
   уточнений финальная пара журналируется с фактическим текстом уточнения;
   состояние вызывающего хранит ответ при исходном вопросе (контекст
   решения), журнал — дословный обмен.
5. Отказные ветки не покрыты — **согласен**: добавлены тесты смерти
   процесса при отказе открытия стрима (пульсом), ветки неподтверждённого
   добития (kill→False), raw-файла с точными байтами (0xFF), профильного
   PATH, строковости env, дословности уточнений, ошибки перезаписи буфера.
   Итог: 105 passed, pytest exit 0.

## Проход критика дословно

Полный проход круга 11 выполнен. Тесты проходят: 98 passed.

1. [ВЫСОКАЯ, подтверждено] Контракт файловых отказов всё ещё неполон:
в PairBuffer._rewrite() запись временного файла, fsync, замена и
синхронизация каталога вне обработки ошибок; аналогично создание каталога
стримов. Сценарий: граф принял пару, перезапись буфера получает
PermissionError — сырой traceback, а оставшаяся пара при повторе уходит
ещё раз. Класс: MVP, порча сырья; потомок находки 2.

2. [ВЫСОКАЯ, подтверждено] Пивот privacy вычищен не полностью: спека
одновременно говорит «гигиена» про каталоги и «по построению вне git» про
буфер; код разрешает буфер в репозитории под игнором; старое обещание в
докстрингах. Два несовместимых критерия. Класс: блокер однозначности;
потомок находки 5.

3. [СРЕДНЯЯ, подтверждено] Raw-свидетельство может исчезнуть молча:
_dump_raw() глотает OSError; ни одного теста на .raw. Класс:
доказательность; потомок находки 6.

4. [СРЕДНЯЯ, подтверждено] Последний обмен после уточнения журналируется с
неверным вопросом: финальное «да» связывается с «Входные ворота», а не с
фактическим текстом уточнения. Дословная история невосстановима. Класс:
аудит слова оператора; потомок находки 7.

5. [ВЫСОКАЯ, подтверждено] Заявленное покрытие отказных веток не
выполнено: тест открытия стрима не проверяет смерть процесса и ветку
неподтверждённого убийства; нет тестов raw-файлов, профильного PATH,
строковых типов env, фактического текста уточнения, ошибок перезаписи
буфера. Класс: MVP-приёмка; потомок.

Тренд: 0 находок на новой территории / 5 потомков прежних правок.
Вердикт: не готова; финализацию решает только оператор.
