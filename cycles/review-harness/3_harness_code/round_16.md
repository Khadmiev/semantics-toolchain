# Круг 16 (третий прогон: код обвязки) — проход критика (sol) и исходы

Дата: 2026-09-01. Версия артефакта: после правок круга 15 (113 тестов).
Тренд: 0 находок новой территории / 2 потомка прежних правок.
Вердикт: не готова к остановке; решает оператор.

## Исходы (разработка; оба — рабочий слой; 116 тестов, pytest exit 0)

1. Маркер не сверяется с последствиями завершения — **согласен**:
   completed=true при отсутствующих исходах — громкая порча завершённой
   истории (не «круг стал незавершённым» с переигрышем состоявшегося
   разбора); висящий запрос у завершённого круга — повреждение контракта
   «одна пачка на круг», не молчаливый пропуск.
2. Поля ремонта журнала не типизированы — **согласен**: entry_gate_answer
   и entry_gate_prompt проверяются как строки в инвариантах меты — список
   вместо дословного ответа не станет строкой «['да']» в журнале.

## Проход критика дословно

Полный проход круга 16 выполнен. Тесты: 113 passed за 81,41 с, код
выхода 0.

1. [ВЫСОКАЯ, подтверждено] Маркер завершённости не сверяется с
обязательными последствиями завершения: завершённые круги выбираются
только среди кругов с существующим файлом исходов, поэтому completed=true
при отсутствующем round_NN_outcomes.md молча игнорируется — восстановление
объявляет круг незавершённым и повторно запускает разработку поверх
состоявшегося разбора. Аналогично completed=true совместим с вновь
появившимся висящим запросом: он попадает в pending_gate_rounds, но цикл
не обслуживает его. Класс: блокер MVP, порча данных; потомок маркера.

2. [СРЕДНЯЯ, подтверждено] Новые поля состояния ремонта журнала не
проходят типовую проверку: _load_meta() не проверяет типы
entry_gate_answer/entry_gate_prompt; повреждённый run.json со списком
вместо ответа даёт в gates.md строковое представление структуры как
дословное слово оператора. Класс: достоверность истории; потомок ремонта.

Тренд: 0 находок на новой территории / 2 потомка прежних правок.
Вердикт: не готова; финализацию решает только оператор.
